pkauth:
  # Use the testkit in-memory SPIs for these integration tests. In production this flag must be
  # absent or false — the starter then refuses to boot without explicit SPI beans.
  dev-mode: true
  relying-party:
    id: example.com
    name: pk-auth starter integration test
    origins:
      - https://example.com
  jwt:
    issuer: pk-auth-test
    audience: pk-auth-test-clients
    # 32-byte deterministic secret so issuer/validator agree across the integration test.
    secret: integration-test-secret-must-be-32-bytes
    token-ttl: PT1H
  ceremony:
    challenge-ttl: PT5M
logging:
  level:
    com.codeheadsystems.pkauth: INFO
